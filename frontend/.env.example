# Desenvolvimento local: deixe VITE_API_URL vazio para usar /api via proxy do Vite.
VITE_API_URL=
VITE_API_PROXY=http://localhost:4000
VITE_DEV_HOST=localhost
VITE_DEV_PORT=5173

# Na Vercel configure somente pelo painel, sem gravar em .env versionado:
# VITE_API_URL=https://SEU-BACKEND.onrender.com
